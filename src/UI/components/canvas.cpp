#include "canvas.hpp"

namespace GVEditor {
    Canvas::Canvas(const int width, const int height)
        : Component(), 
          _width(width), 
          _height(height)
    {
        // impl
    }
}
