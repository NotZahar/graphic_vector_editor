#include "exit_controller.hpp"

namespace GVEditor {
    ExitController::ExitController()
    {
        // connect(view, ..., this, ...)
        // impl
    }

    void ExitController::onExit() {
        // emit cancel();
    }
}