#include "layout.hpp"

namespace GVEditor {
    void Layout::addComponent(std::unique_ptr<Component>) {
        // impl
    }

    void Layout::removeComponent(unsigned int) {
        // impl
    }
}
